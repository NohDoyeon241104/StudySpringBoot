# # spring.application.name=todoapp
# # Spring Data Source설정 - MySQL DB 접속 정보를 설정해준다.

# == .yml 일 때 ==
# spring:
#   application:
#     name: todoapp
  
#   datasource:
#     url: jdbc:mysql://localhost:3307/todoapp?useSSL=false&serverTimezone=UTC
#     username: root
#     password: root
#     driver-class-name: com.mysql.cj.jdbc.Driver

#   # 여기 jpa의 시작 위치가 datasource와 같아야 합니다!
#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true
#     properties:
#       hibernate:
#         format_sql: true

# Application Name
spring.application.name=todoapp

# MySQL 연결 (3307 포트)
spring.datasource.url=jdbc:mysql://localhost:3307/todoapp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정 - 'create'로 변경하여 강제 생성 시도
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# MySQL 8 버전에 맞는 다이얼렉트 명시
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect